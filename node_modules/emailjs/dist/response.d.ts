import type { Socket } from 'net';
import type { TLSSocket } from 'tls';
export declare class SMTPResponseMonitor {
    readonly stop: (err?: Error) => void;
    constructor(stream: Socket | TLSSocket, timeout: number, onerror: (err: Error) => void);
}
